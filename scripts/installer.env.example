# Installer secrets/config (do not commit real values)
# Copy to: /root/.config/bedolaga/installer.env
# chmod 600 /root/.config/bedolaga/installer.env

# Fine-grained GitHub token with:
# - Repository access: spiderbot, spidercabinet, install-spider-bot
# - Permissions: Contents (Read-only), Metadata (Read-only)
GITHUB_TOKEN=ghp_replace_me
